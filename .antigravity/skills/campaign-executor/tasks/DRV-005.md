# Task DRV-005: Unit Tests for `src/sdk/search/mod.rs`

**Description:** `src/sdk/search/mod.rs` (845L) is the critical hybrid search orchestration module with **0 tests**. It contains 4 core functions that route search queries between text (BM25), vector (HNSW), and hybrid (RRF) pipelines. Write comprehensive unit tests.

**The 4 functions to test:**

1. `pub fn search()` (L27) — Public router: validates input, routes to lexical/vector/hybrid based on request payload, supports explain mode with per-hit breakdowns.

2. `fn lexical_search()` (L140-290) — BM25 text search: loads namespace stats, term stats, doc stats; computes TF-IDF scores; iterates postings via `engine.scan_partition_prefix_iter(BackendPartition::TextIndex, ...)`.

3. `fn vector_memory_search()` (L292-381) — Vector search via HNSW with brute-force fallback: `hnsw.search_nearest()` then `engine.get_many()` for node hydration, filter matching via `matches_memory_filters()`, auto-tuning report on success/fallback.

4. `fn hybrid_search()` (L383-407) — RRF fusion: calls both lexical_search + vector_memory_search, then `crate::planner::fuse_rrf()` and truncate.

**Test helpers already available:**
- `debug_corrupt_derived_index_state_for_tests()` (L592)
- `debug_clear_derived_indexes_for_tests()` (L612)
- `debug_corrupt_text_index_state_for_tests()` (L623)
- `debug_clear_text_index_for_tests()` (L637)
- `debug_corrupt_text_index_posting_tf_for_tests()` (L659)
- `debug_corrupt_text_index_posting_positions_for_tests()` (L681)
- `debug_corrupt_text_index_term_stats_for_tests()` (L695)
- `debug_corrupt_text_index_doc_stats_for_tests()` (L716)
- `debug_text_index_posting_keys_for_tests()` (L730)
- `debug_memory_search_plan_for_tests()` (L756)

**Key test scenarios (cover all):**

| Scenario | Functions | Edge case |
|----------|-----------|-----------|
| Empty search (no text, no vector) | `search()` | Returns `Ok(Vec::new())` |
| top_k=0 | `search()`, `lexical_search()`, `hybrid_search()` | Early return empty |
| Text-only search | `search()` → `lexical_search()` | Terms found, terms not found, filters applied |
| Vector-only search | `search()` → `vector_memory_search()` | HNSW success, brute-force fallback (empty HNSW) |
| Hybrid search | `search()` → `hybrid_search()` | RRF fusion with lexical+vector results |
| Explain mode | `search()` | Returns explained hits with breakdown |
| Invalid namespace | `search()` | `validate_namespace` error |
| Invalid filters | `search()` | `validate_metadata` error |
| BM25 scoring | `lexical_search()` | Verify IDF calculation (standard BM25Okapi) |
| Corrupt text index | `lexical_search()` | Returns `SearchError` with `rebuild_index` hint |
| Namespace with no records | `lexical_search()` | Early return empty (doc_count == 0) |

**Verification:**
- `cargo test --package vantadb --lib -- sdk::search` — all pass
- `cargo check -p vantadb` — clean
- No external test dependencies added

**Acceptance criteria:**
- [ ] At least 15 test cases covering all 4 functions
- [ ] Edge cases: empty inputs, top_k=0, namespace not found, corrupt index
- [ ] HNSW→brute-force fallback path tested
- [ ] RRF fusion correctness tested (lexical+vector both return results)
- [ ] All existing tests still pass

**Files:** `src/sdk/search/mod.rs` (add `#[cfg(test)] mod tests { ... }` at EOF)

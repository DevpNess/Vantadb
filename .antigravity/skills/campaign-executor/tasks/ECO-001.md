# ECO-001: Remove dead Claude Code hooks (C1)

## Type
Cleanup — dead code removal

## Problem
`.antigravity/hooks/hooks.json` y `session-start.sh` son artefactos de Claude Code que **nunca se ejecutan** en OpenCode sobre Windows:
- `hooks.json` usa el formato PascalCase de Claude Code (`SessionStart`), no el JS plugin API de OpenCode
- `session-start.sh` requiere `bash` y `jq` — ninguno disponible en Windows por defecto
- Usa variables de entorno `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PROJECT_DIR` que OpenCode no define
- El contenido de `using-agent-skills` (lo que intenta inyectar) ya está duplicado en AGENTS.md

## Investigation Notes (2026-07-23)
- OpenCode NO lee `hooks.json` — usa plugin API con hooks como `experimental.chat.system.transform`
- OpenCode no tiene `SessionStart` hook — tiene `session.created` vía plugin JS
- No hay ningún `.ps1` equivalente — el hook nunca fue portado
- `using-agent-skills/SKILL.md` (191 líneas) está disponible manualmente vía `skill using-agent-skills`
- Blast radius de eliminar: **CERO** — ningún flujo, comando, agente o prompt depende de estos archivos
- Ver reporte completo de investigación en sesión C1

## Contract
`.antigravity/hooks/` contiene solo 2 archivos que son dead code. El directorio puede eliminarse o dejarse vacío.

## Herramientas
- git (rm, commit)

## Steps
### Step 1: Eliminar `hooks/hooks.json`
- **Archivos:** `.antigravity/hooks/hooks.json`
- **Acción:** `git rm .antigravity/hooks/hooks.json`
- **Verify:** `Test-Path ".antigravity/hooks/hooks.json"` → False
- **Estado:** ⬜ PENDING

### Step 2: Eliminar `hooks/session-start.sh`
- **Archivos:** `.antigravity/hooks/session-start.sh`
- **Acción:** `git rm .antigravity/hooks/session-start.sh`
- **Verify:** `Test-Path ".antigravity/hooks/session-start.sh"` → False
- **Estado:** ⬜ PENDING

### Step 3: Eliminar directorio `hooks/` si quedó vacío
- **Archivos:** `.antigravity/hooks/`
- **Acción:** `Remove-Item ".antigravity/hooks"` si está vacío tras rm
- **Verify:** `Test-Path ".antigravity/hooks"` → False
- **Estado:** ⬜ PENDING

### Step 4: Commit
- **Acción:** `git commit -m "chore: remove dead Claude Code hooks (ECO-001)"`
- **Verify:** `git log --oneline -1` muestra el mensaje correcto
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna

## Notas
- Los hooks existen pero son 100% inertes en OpenCode/Windows
- Si en futuro se necesita auto-inyección de meta-skills, debe implementarse como plugin JS OpenCode (como ponytail.mjs), no como hooks.json/bash
- Los 38 bytes de hooks.json y 475 bytes de session-start.sh liberados

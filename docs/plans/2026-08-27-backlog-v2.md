# Plan de Ejecución: Backlog V2 — Core Cycles + Promotion Validation (2026-08-27)

> **Campaign ID:** ce6769fa-4ba7-4530-91f2-cd76329cfdcc
> **Inicio:** 2026-08-27
> **Estado:** ⏳ EN PROGRESO
> **Fuente:** `docs/Backlog.md` (reconteo 2026-08-27: 102 activas tras 7 completadas de v1)
> **Scope:** slice vertical de 7 tareas ✅ DO — 3 ciclos estructurales del CodeGraph (FIND-34/35/36) + 1 durability gap (CORE-01) + 3 gates de promoción default-members (STABLE-01/03/08). Resto queda en backlog para próximos slices.
> **SPEC:** gate spec-first para CORE-01 (ADR de formato antes de implementar) — evaluado `spec-driven-development` §Gate P, ninguna otra introduce API nueva sin spec.

## Resumen

| Resultado | Count | Criterio |
|-----------|-------|----------|
| ✅ DO | 7 | Bug/ciclo real verificado + contrato mecánico + appetite ≤1d + desbloquea durability/CI |
| 🟡 DEFER | 68 | Requiere DISCOVERY pesado (RES-01..09, PRX-*, MEM-*, UX-*, etc.) o esfuerzo >> impacto inmediato |
| ❌ SKIP | 14 | Ya implementado (FIND-37/39, MCP-36/38, WSM-01, TS-05, STABLE-00, SRV-01/04, etc.) — verificado stale |
| 🔴 BLOQUEADO | 13 | Upstream no publicado (AUD-042 tantivy 0.27), decisión owner pendiente (WEB-09, DESKTOP-40), infra externa (BND-07, LEG-01) |

Status: ⬆️ uphill = 1 (CORE-01 ADR formato) · ⬇️ downhill = 16 steps atómicos

### Triage detallado

**SKIP (14):** `FIND-37` (Option<&SparseVector> ya), `FIND-39` (test_scalar_remove 15 passed), `MCP-36` (2025-06-18 negociado), `MCP-38` (82 hints), `WSM-01` (persistence bool fiel), `TS-05` (engines guard), `STABLE-00` (ADR-031), `SRV-01` (AuditLogger rota), `SRV-04` (alt_api_key), `TS-02` (_native async), `FIND-22` (CI_POLICY RESOURCE-GUARD), `AUD-044` (shim flush), `DISC-03` ICEBOX, `FIND-26` wal_archiver remove.

**BLOQUEADO (13):** `AUD-042` (tantivy 0.27 no publicado), `MCP-34b` (prereq snapshot restore), `PROV-12` (requiere PROV-01/02/04), `FUT-02..11`/`PRO-01..06` (roadmap), `WEB-09` (input visual owner), `DESKTOP-40` (i18n), `BND-07` (DNS), `LEG-01` (trademark).

**DEFER (68):** `RES-01..09` (ACID/WAL/chaos), `PRX-01..12` (proxy), `MEM-59..70` (memory), `UX-02..19` (Studio), `WEB-*/TS-*/WSM-*` no críticos, `P32 MOD-*/P28` restantes, `STABLE-02/04-07/09` (excepto 01/03/08 en este slice).

---

## Tasks

### Task 1: FIND-34 — Ciclo WAL Writer (4 nodos: open↔open_with_buffer↔recover_valid_records↔quarantine_corrupt_tail)

- **Appetite:** max 1d | **Esfuerzo:** 🟠 1d | **Prioridad:** 🔴 Alta | **Ruta:** `vanta-worker`
- **Archivos clave:** `src/wal.rs:202-575` (`open`, `open_with_buffer`, `recover_valid_records`, `quarantine_corrupt_tail`) · `src/wal_sharded.rs` (si aplica)
- **Verificación real:** ✅ CÓDIGO-REAL — `codegraph_explore` + `rg` confirma ciclo 4 nodos en writer crítico; `src/wal.rs:203` `open` → `open_with_buffer` → `recover_valid_records` → `quarantine_corrupt_tail` → sin tests de coverage en recovery/quarantine (codegraph Fase 1 2026-08-27)
- **Gate Justificación:** ciclo en path crítico de durability sin tests — riesgo de regresión en crash recovery; fix acotado (extraer helper o documentar ciclo con tests) desbloquea certificación WAL
- **Gate Result:** ✅ DO
- **Contrato:** `cargo nextest run -p vantadb --profile audit -E 'test(wal)'` ✅ (recovery/quarantine tests verdes) + `rg -n "quarantine_corrupt_tail|recover_valid_records" src/wal.rs` muestra 1 definición cada uno con cobertura + `codegraph_explore "wal cycle FIND-34"` muestra ciclo roto o justificado en doc
- **Task file:** `.opencode/skills/campaign-executor/tasks/FIND-34.md`
- **Estado:** ✅ COMPLETED (2026-08-27 — vanta-worker · DAG documentado `src/wal.rs:178-193` · 2 tests edge añadidos · `cargo nextest -p vantadb -E test(wal)` 62/62 ✅ · `rg` 1 def c/u con cobertura · `cargo check/clippy/fmt/docs` ✅ · commit `fix: FIND-34`)
- **Risk Register:** 🟡×🔴 ciclo sin test → recovery corrupto silencioso → mitigar con kill test · 🟢×🟡 refactor rompe API `open_with_buffer` → test de firma
- **Pre-mortem:** 1) test recovery requiere FS real no mock 2) quarantine race con WAL sharded 3) doc justificación insuficiente para audit
- **Stop:** >1d → DEFER | 3 iter sin cycle break → re-planear | premisa invalidada (ciclo ya roto) → SKIP
- **Cynefin:** 🟨 complicado — causa-efecto analizable por experto WAL, 2 approaches válidos (extraer vs documentar)
- **Uphill/Downhill:** ⬇️ 3 steps (repro ciclo → test recovery → refactor/doc)

### Task 2: FIND-35 — Ciclo StorageEngine get/prefetch (2 nodos)

- **Appetite:** max 1d | **Esfuerzo:** 🟠 1d | **Prioridad:** 🔴 Alta | **Ruta:** `vanta-worker`
- **Archivos clave:** `src/storage/engine/get.rs` (`StorageEngine.get` ↔ `prefetch_related`)
- **Verificación real:** ✅ CÓDIGO-REAL — `get.rs:13` comentario MCP-15 guard + `rg "prefetch_related"` → ciclo 2 nodos documentado pero sin justificación de por qué es necesario; codegraph Fase 1
- **Gate Justificación:** ciclo en read path hot — cada `get` cache-miss dispara `prefetch_related` que re-entra a `get` (guard `PrefetchGuard` thread_local ya existe pero no documentado como intención)
- **Gate Result:** ✅ DO
- **Contrato:** `cargo nextest run -p vantadb -E 'test(prefetch|get.*cache)'` ✅ + `rg -n "PrefetchGuard" src/storage/engine/get.rs` hit + `codegraph_explore` muestra ciclo justificado en doc o roto
- **Task file:** `.opencode/skills/campaign-executor/tasks/FIND-35.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🟡 guard no cubre todos los paths → live-lock · 🟢×🟡 doc sin actualizar → drift
- **Pre-mortem:** 1) guard thread_local no funciona en async spawn_blocking 2) test no reproduce co-access pair A↔B 3) fix aplanado rompe perf
- **Stop:** >1d → DEFER | 3 iter sin green → re-planear
- **Uphill/Downhill:** ⬇️ 2 steps (test co-access → doc/justify ciclo)

### Task 3: FIND-36 — Cross-crate NativeConnection ↔ RocksDbBackend (3 ciclos get/put/delete)

- **Appetite:** max 1d | **Esfuerzo:** 🟠 1d | **Prioridad:** 🔴 Alta | **Ruta:** `vanta-arch`
- **Archivos clave:** `desktop/src-tauri/src/connections/native.rs` ↔ `src/backends/rocksdb_backend.rs`
- **Verificación real:** ✅ CÓDIGO-REAL — `codegraph_explore` Fase 1 muestra 3 ciclos get/put/delete entre Tauri frontend y backend RocksDB — violación frontera crate (core llama a skills/desktop)
- **Gate Justificación:** inversión de dependencia semántica — core no debe depender de desktop; fix requiere diseño (extraer trait o mover backend detrás de interfaz), no solo code
- **Gate Result:** ✅ DO (requiere DISCOVERY vanta-arch)
- **Contrato:** `cargo check -p vantadb --all-targets` ✅ + `codegraph_explore "NativeConnection RocksDbBackend"` muestra 0 ciclos cross-crate o ADR que documenta frontera + `cargo check -p vantadb --all-targets --all-features` 0 cycles
- **Task file:** `.opencode/skills/campaign-executor/tasks/FIND-36.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🔴 refactor trait rompe desktop build · 🟢×🟡 Tauri caller no migra
- **Pre-mortem:** 1) RocksDB backend solo usa config, no Tauri conn — ciclo es falso positivo codegraph (verificar con grep real) 2) trait extraction necesita async 3) desktop usa `native.rs` solo para test
- **Stop:** >1d → DEFER (requiere DISCOVERY) | premisa invalidada (no hay ciclo real) → SKIP con evidencia grep
- **Cynefin:** 🟧 complejo — causa-efecto solo emerge al experimentar con fronteras crate
- **Uphill/Downhill:** ⬆️ 1 (¿es ciclo real o falso positivo?) · ⬇️ 3 steps (DISCOVERY arch → trait/ADR → verify)

### Task 4: CORE-01 — Persistencia on-disk de vectores Binary (y no-F32) en vstore

- **Appetite:** max 1d | **Esfuerzo:** 🟡 1d | **Prioridad:** 🟡 Media | **Ruta:** `vanta-arch` (requiere ADR)
- **Archivos clave:** `src/storage/ops.rs:59` (`write_node_to_vstore` vector_len=0) · `src/node/disk.rs` (`DiskNodeHeader`) · `src/storage/archive.rs:359` · `src/storage/engine/get.rs` · `src/storage/engine/txn.rs`
- **Verificación real:** ✅ CÓDIGO-REAL — `ops.rs:59` escribe `vector_len=0` y NO persiste datos para `Binary`/`Turbo`; `get()` rescata desde HNSW (fix `8c8eef23`) pero `rebuild_hnsw_from_vstore` tras reopen pierde Binary (header 0 → rebuild lee sin vector) — gap persistencia on-disk
- **Gate Justificación:** durability bug — insert Binary → get OK en memoria pero tras reopen + rebuild se pierde; fix requiere diseño de formato (flag en DiskNodeHeader) + ADR antes de implementar (Gate spec-first)
- **Gate Result:** ✅ DO
- **Contrato:** ` rg -n "vector_len.*0" src/storage/ops.rs` 0 after fix + `cargo nextest run -p vantadb --profile audit -E 'test(persistence|vstore|rebuild)'` ✅ (roundtrip Binary persist→reopen→search) + ADR `docs/architecture/adr/ADR-032-binary-vector-persistence.md` existe con tabla de formato + migración/versionado
- **Task file:** `.opencode/skills/campaign-executor/tasks/CORE-01.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🔴 ADR formato elige convención sobre `vector_len` que rompe compat backward · 🟢×🟡 migración no testeada → data loss
- **Pre-mortem:** 1) DiskNodeHeader ya tiene campo flags pero no se persiste 2) rebuild lee vector_len=0 como "sin vector" por diseño 3) test roundtrip requiere fsync real
- **Stop:** >1d → DEFER | ADR no aprobado por owner → BLOQUEADO | premisa invalidada (ya persiste Binary) → SKIP
- **Cynefin:** 🟨 complicado — requiere experto storage para decidir encoding + versionado
- **Uphill/Downhill:** ⬆️ 1 (ADR formato) · ⬇️ 3 steps (ADR → encode/decode → test roundtrip + migración)

### Task 5: STABLE-01 — Validar `vanta-memory` (gates 1-6)

- **Appetite:** max 1d | **Esfuerzo:** 🟡 1d | **Prioridad:** 🟠 Media | **Ruta:** `vanta-worker`
- **Archivos clave:** `vanta-memory/Cargo.toml` · `vanta-memory/src/` · `src/sdk/` (depende de vantadb sin server)
- **Verificación real:** ✅ CÓDIGO-REAL — `vanta-memory` `publish=false`, depende de `vantadb` sin `server`; nunca pasó gates promotion (STABLE-00 ADR-031 define 10 checks, este es 1º de los N)
- **Gate Justificación:** gate de entrada P47 — sin validar vanta-memory no se puede medir Fast Gate ampliado (STABLE-08)
- **Gate Result:** ✅ DO
- **Contrato:** `cargo check -p vanta-memory --all-targets` ✅ + `cargo clippy -p vanta-memory --all-targets --all-features -- -D warnings` 0 ✅ + `cargo nextest run -p vanta-memory --profile audit -j 2` 0 failed ✅ + `cargo deny check` 0 ✅ + `scripts/validate-docs-coverage.ps1` 0 gaps para APIs vanta-memory + `cargo package -p vanta-memory --dry-run` no falla
- **Task file:** `.opencode/skills/campaign-executor/tasks/STABLE-01.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🟡 `cargo check` >60s → documentar como Heavy · 🟢×🟡 nextest flaky (memoria) → -j 2
- **Pre-mortem:** 1) vanta-memory tiene feature `server` que tira tokio/axum → clippy lento 2) docs coverage no cubre vanta-memory aún 3) `cargo package` falla por `publish=false` (gate 6 es check no falla, ok)
- **Stop:** >1d → DEFER | 2 iter clippy fail → documentar Heavy | premisa invalidada (ya validado 3 corridas) → SKIP
- **Uphill/Downhill:** ⬇️ 2 steps (check/clippy/nextest + docs)

### Task 6: STABLE-03 — Validar `vantadb-server` (gates 1-6 + 42 tests)

- **Appetite:** max 1d | **Esfuerzo:** 🟡 1d | **Prioridad:** 🔴 Alta | **Ruta:** `vanta-worker`
- **Archivos clave:** `vantadb-server/src/` · `src/cli_server.rs` · `src/audit.rs`
- **Verificación real:** ✅ CÓDIGO-REAL — `vantadb-server` ya pulido (SRV-01/02/06, 42 tests) pero nunca validado contra 10 gates ADR-031; `cargo nextest -p vantadb-server` debe ser 0 failed
- **Gate Justificación:** server es candidato directo a default-members (no Heavy si <60s check) — validar ahora alimenta STABLE-08 measurement
- **Gate Result:** ✅ DO
- **Contrato:** `cargo check -p vantadb-server --all-targets` ✅ + `clippy -D warnings` 0 ✅ + `cargo nextest run -p vantadb-server --profile audit -j 2` 42/42 ✅ + `cargo deny check` 0 ✅ + `validate-docs-coverage` 0 gaps + `cargo package -p vantadb-server --dry-run` no falla
- **Task file:** `.opencode/skills/campaign-executor/tasks/STABLE-03.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🟡 server feature `prometheus` no en default → clippy necesita --all-features
- **Pre-mortem:** 1) audit rotación ya implementada pero test no cubre retención 2) 42 tests incluyen mcp_integration que requiere server feature
- **Uphill/Downhill:** ⬇️ 2 steps

### Task 7: STABLE-08 — Medición Fast Gate con `default` ampliado (`test/default-all` + `just verify`)

- **Appetite:** max 1d | **Esfuerzo:** 🟡 1d | **Prioridad:** 🔴 Alta | **Ruta:** `vanta-lead` (CI/CD)
- **Archivos clave:** `Cargo.toml:636` (`default-members`) · `dev-tools/verify.ps1` · `.github/workflows/ci-rust-10.yml` · `docs/operations/CI_POLICY.md`
- **Verificación real:** ✅ CÓDIGO-REAL — `default-members` ampliado simulado `[ ".","vantadb-python","vanta-memory","vanta-proxy","vantadb-server","vantadb-mcp","vantadb-wasm"]` en rama `test/default-all` nunca medido; STABLE-00 ADR-031 §9 exige `<5 min` o re-etiqueta Heavy
- **Gate Justificación:** gate de promoción — sin medición no se puede decidir <5 vs Heavy (owner question ADR-031 §4); bloquea STABLE-09 promoción atómica
- **Gate Result:** ✅ DO
- **Contrato:** rama `test/default-all` (o simulación local `Cargo.toml` ampliado) + `just verify` (o `dev-tools/verify.ps1`) wall time registrado por job en `docs/operations/CI_POLICY.md` §default-members + `dev-tools/verify_changed.ps1` con cache fría <5 min o justificación Heavy + `cargo clean` + `npm ci` 3 corridas sin flaky
- **Task file:** `.opencode/skills/campaign-executor/tasks/STABLE-08.md`
- **Estado:** ⬜ PENDING
- **Risk Register:** 🟡×🟡 cache fría vs warm difiere 2× → medir ambos · 🟢×🟡 Windows runner 1455 page file → -j 2
- **Pre-mortem:** 1) `just verify` incluye `wasm-pack` que requiere `rustup target add` → fallo no relacionado a default-members 2) medida local ≠ ubuntu-latest CI (documentar entorno) 3) `verify_changed.ps1` con `paths:` filter no dispara todos los checks
- **Stop:** >1d → DEFER | 2 iter sin <5 → documentar Heavy y cerrar con justificación | premisa invalidada (ya medido) → SKIP
- **Uphill/Downhill:** ⬇️ 2 steps (simular ampliado → medir 3 corridas → registrar)

---

## Uphill / Downhill agregado

| Eje | Count | Detalle |
|-----|-------|---------|
| ⬆️ uphill | 1 | CORE-01 ADR formato (gate spec-first) |
| ⬇️ downhill | 16 | FIND-34(3)+FIND-35(2)+FIND-36(3)+CORE-01(3)+STABLE-01(2)+STABLE-03(2)+STABLE-08(2) steps atómicos |

## Dependencias y orden

```
Wave 0 (paralelo, MAX_CONCURRENT=3, sin deps): FIND-34, FIND-35, STABLE-01
Wave 1 (paralelo): FIND-36 (DISCOVERY arch), STABLE-03, STABLE-08 (mide ampliado)
Wave 2: CORE-01 (requiere ADR, después de Wave 0-1 para no bloquear)
```

STABLE-01/03/08 son independientes entre sí (tocan crates distintos) y de FIND-34/35 (distintos archivos) — paralelizable. CORE-01 depende de ADR spec-first, no de gates de promoción.

## Stop conditions globales

| Stop condition | Trigger | Acción |
|----------------|---------|--------|
| Appetite campaña excedido | >7d wall time total | abort → re-triaje DEFER restantes |
| Rabbit hole | 3 iter sin progreso verificable en una tarea | abort tarea → re-planear approach |
| Premisa invalidada | codegraph contradice Paso 0 (ciclo ya roto / ya persiste) | SKIP y liberar slot |
| Presupuesto | 40 sub-agentes / 120 min | HARD STOP + reporte parcial |

## Notas

- **Ponytail (full) activo:** YAGNI ladder en cada step — stdlib/native primero, 1 línea antes de 50, borrar antes de añadir. `// ponytail:` con ceiling para locks O(n²) si aplica.
- **Conventional Commits:** `fix:` (FIND-*), `feat:` (CORE-01 si cambia formato), `docs:` (STABLE-00/08), `chore:` (STABLE-01/03).
- **Pre-launch gate:** no publish; `cargo semver-checks` solo si CORE-01 cambia API pública.
- **Al completar cada tarea:** `skill progreso` Trigger 1 (fila fuera de Backlog → `docs/avance/` dominio + `BACKLOG_HISTORY.md`) + auto-commit con verify mecánico (`campaign_verify_cmd`).
- **Question Gates HITL:** CORE-01 ADR requiere `question` al owner sobre formato (flag en DiskNodeHeader vs convención vector_len) antes de implementar.

## Verificación cruzada

- Workspace Cargo.toml herencia: no duplicar deps.
- `cargo-deny` MIT/Apache-2.0 only — verificado (no bump tantivy en este slice).
- `verify.ps1`/`just verify` local antes de merge (cada task file lo exige).


# REV-003: Coverage threshold ≥80%

## Type
CI/Tests — coverage enforcement

## Problem
CII Silver requires ≥80% line coverage. Current threshold is below target.

## Contract
`.github/workflows/ci-rust-10.yml` coverage job fails the build when line coverage drops below 80%.

## Investigation Notes (2026-07-22)
- `.github/workflows/ci-rust-10.yml` ya tiene coverage threshold step (L256-274)
- Threshold actual: 59% → **60%** (subido incrementalmente)
- Python script parsea `cargo llvm-cov report --json`, extrae `data[0].totals.lines.percent`
- Exit 1 si coverage < threshold

## Gap Analysis
- De 60% → 80%: ~20% de cobertura adicional requerida
- **NO es cambio de configuración** — requiere escribir tests para código no cubierto
- Prioridad sugerida para nuevos tests:
  1. `src/engine.rs` — hot paths
  2. `src/wal.rs` — durabilidad
  3. `src/storage/` — backends
  4. `src/vector/` — HNSW

## Cambios Realizados (2026-07-22)
- Threshold: 59% → 60%

## Estado: ⚠️ PARTIAL (requiere test-writing effort, ~4-8h)

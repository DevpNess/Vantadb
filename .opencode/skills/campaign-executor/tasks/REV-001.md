# REV-001: CI Rust TSan ABI Mismatch

## Type
CI/DevOps — GitHub Actions YAML

## Problem
Original: `sanitizer-tsan` job in `.github/workflows/ci-rust-10.yml` uses `-Zsanitizer=thread` and `-Cunsafe-allow-abi-mismatch=sanitizer` flags.

## Analysis (2026-07-22)
- Los flags `-Zsanitizer=thread` y `-Cunsafe-allow-abi-mismatch=sanitizer` son **necesarios** para que TSan funcione — no son configurables ni removibles sin perder el job.
- El job ya está correctamente configurado como BEST-EFFORT:
  - `continue-on-error: true` con comentarios explicatorios (L422)
  - `|| echo "TSan reported issues..."` como fallback no-fatal
  - Nightly toolchain (inherentemente inestable)
  - `TSAN_OPTIONS: halt_on_error=0` (L451)
- No requiere cambios. Estado actual es el correcto.

## Contract
- `.github/workflows/ci-rust-10.yml:416-451` — job TSan con continue-on-error, BEST-EFFORT, nightly, error handling
- YAML sintácticamente válido
- No hay flags redundantes ni configuración mejorable

## Estado: ✅ COMPLETED (2026-07-22)

## Context Save (run 2026-08-11, lead directo)
- Commit: 339107b0 — fix(ERR-031)
- Fix: trait `VecIndex::add` (src/index/mod.rs:65, `#[allow(dead_code)]`) devolvía `()` — cualquier rechazo se tragaba. Cambio a `Result<()>`:
  - FlatIndex: infalible → Ok(())
  - ScannIndex: non-full vector → ValidationError (antes `return` silencioso); dim mismatch → ValidationError
  - DiskAnnIndex: non-full → ValidationError (antes `return`)
  - IvfIndex: read-only add → ValidationError (antes `panic!`)
  - CPIndex (search.rs): propaga error real de CPIndex::add (antes warn-and-drop)
  - scann lazy rebuild (search.rs:735): `if let Err(e)` → warn
- Verify: cargo check --jobs 2 ok; index:: suite 251/251; clippy/fmt ok.
- Nota: el trait sigue `#[allow(dead_code)]` — es contrato futuro para Arc<dyn VecIndex>; ahora no puede perder inserts silenciosamente.
# AUD-043: Fix clippy unused variable `ns` en cli_server.rs (gate pre-push roto)

## Metadata
- **Plan file:** docs/plans/2026-08-28-backlog-triage.md
- **Creado:** 2026-08-28
- **last-synced:** 2026-08-28
- **Estado:** ✅ COMPLETED
- **Prioridad:** 🔴 Alta

## Blast Radius
- **Archivo:** `src/cli_server.rs:1507` (`options_for` closure)
- **Cambio:** parámetro `ns` → `_ns` (underscore prefix para indicar uso intencional)
- **Callers:** `list_all_namespaces` handler (mismo archivo)
- **Impacto:** Cero — cambio cosmético solo para satisfacer clippy `-D unused_variables`

## Verificación (DISCOVERY)
- `cargo clippy -p vantadb -- -D warnings` → **exit 0** (sin warnings `unused_variables`)
- Línea 1507 ya muestra `move |_ns: String|` — fix ya aplicado en disco
- Backlog lo marcaba Pendiente por auditoría anterior (audit-full-20260825-010607) pero gap ya cerrado

## Contrato (verificación mecánica obligatoria)
- `cargo check -p vantadb` → exit 0 ✅
- `cargo clippy -p vantadb -- -D warnings` → exit 0 ✅
- `cargo fmt --check -p vantadb` → limpio ✅
- Contrato específico: `cargo clippy -p vantadb -- -D warnings 2>&1 | Select-String "unused variable.*ns" | Measure-Object | Select-Object Count` == 0 ✅

## Herramientas
- cargo check/clippy/fmt

## Steps
### Step 1: Discovery — verificar estado actual
- **Archivos:** `src/cli_server.rs:1507`
- **Acción:** confirmar que clippy no reporta `unused variable.*ns` y que parámetro ya es `_ns`
- **Verify:** `cargo clippy -p vantadb -- -D warnings` ✅ (exit 0)
- **Estado:** ✅ COMPLETED

### Step 2: Cierre idempotente — verificación completa
- **Archivos:** — (solo verify)
- **Acción:** confirmar que todo pasa sin edición (fix ya en disco)
- **Verify:**
  - `cargo check -p vantadb` → exit 0 ✅
  - `cargo clippy -p vantadb -- -D warnings` → exit 0 ✅
  - `cargo fmt --check -p vantadb` → limpio ✅
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna

## Notas
- Tarea **idempotente**: el fix ya estaba aplicado (`_ns` en línea 1507)
- No se requiere edición, commit, ni migración de backlog
- El task file previo en `.opencode/skills/campaign-executor/tasks/AUD-043.md` era para otro issue (wasm `collect_all_deduped`) — este es el task file correcto para el clippy fix

## Context Save Point
- **Fecha:** 2026-08-28
- **Branch:** develop
- **Verificación:** check ✅ / clippy ✅ / fmt ✅ — todo exit 0
- **Próxima tarea:** REVIEW-07 (según plan wave 0)
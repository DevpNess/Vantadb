# AUD-043: `collect_all_deduped` O(n) HashSet<String,String> — dedup con u128 node-ids

## Metadata
- **Plan file:** docs/plans/2026-08-12-audit-full.md (hallazgos pendientes, audit-full-20260812-231204)
- **Creado:** 2026-08-16T00:00
- **last-synced:** 2026-08-16T00:00
- **Estado:** ✅ COMPLETED
- **Prioridad:** Baja (P2-8)

## Blast Radius
- **Callees:** `self.inner.list_namespaces()` (core), `self.inner.list(ns, opts)` (core, paginado con cursor), `enter(&self.op_gate)`, `to_js_err`
- **Callers:** `persist_payload` (vantadb-wasm/src/lib.rs:657, único caller vía `self.collect_all_deduped()?` en :673) → usado por `save` (:722) y `save_idb` (:747) (PERF-08 differential-persist)
- **Tests que cubren el comportamiento:** NINGUNO directo. Tests de persist/save en el crate son de browser (`#[wasm_bindgen_test]`), ninguno cubre dedup de `collect_all_deduped`
- **Implicaciones:** cambio puramente interno a la función; dedup por `record.node_id` (u128 Copy, cero alocación por record) vs `(String,String)` (2 alocaciones por record). Lógica de paginación (cursor, limit 10_000, MAX_RECORDS guard) se preserva intacta. Cero cambio de comportamiento observable.

## Verificación de determinismo de node_id (DISCOVERY)
- `memory_node_id(namespace, key)` = `XxHash3_128` sobre `namespace\0key` — **src/sdk/serialization/mod.rs:54-60**
- Unit test existente `test_memory_node_id_deterministic` (mod.rs:572-583) prueba determinismo + diferenciación por ns/key
- `VantaMemoryRecord.node_id` se puebla en TODA inserción/lectura (19 callers de `memory_node_id`: put_one, put_batch_inner, get, delete, import, etc.) — los records devueltos por `list()` ya traen node_id correcto
- **Veredicto:** 1:1 determinístico (ns,key) → u128, colisión 2^-128 (despreciable). Dedup por `record.node_id` es equivalente a (ns,key) sin alocar

## Impacto mapeado (Regla 0)
- **Archivos que se modifican:** `vantadb-wasm/src/lib.rs` — solo `collect_all_deduped` (:556-588, 3 líneas del HashSet) + test unitario nuevo en `mod tests`
- **Referencias entrantes:** `collect_all_deduped` → `persist_payload` (único caller, mismo archivo :673)
- **Referencias salientes:** `self.inner.list_namespaces` / `self.inner.list` (core, NO se tocan). `record.node_id` — campo existente de `VantaMemoryRecord`, sin cambios
- **Impacto si se elimina:** N/A (optimización, no eliminación). Veredicto: cambio seguro, mismo comportamiento
- **Dominio:** vantadb-wasm → reglas js-ecosystem.md (R-1..R-4) — ninguna bloquea el cambio; no toca persistencia real (sigue InMemory), no toca API pública, no toca op-gate/drain
- **Git history:** working tree tiene cambios sin commit en `AUD-024.md` y `docs/Backlog.md` (del orquestador) — NO tocar esos archivos

## Contrato (verificación mecánica obligatoria)
- `cargo check -p vantadb-wasm` → exit 0
- `cargo clippy -p vantadb-wasm --all-targets --all-features -- -D warnings` → exit 0
- `cargo nextest run --profile audit -p vantadb-wasm --build-jobs 2` → verde
- `cargo fmt --check` sobre `vantadb-wasm/src/lib.rs` → limpio
- Test unitario mínimo: `collect_all_deduped` no produce duplicados (ns,key) en output

## Herramientas
- codegraph (blast radius verificado), terminal cargo

## Steps
### Step 1: Discovery — verificar determinismo de node_id
- **Archivos:** `src/sdk/serialization/mod.rs:54-60`, `vantadb/src/sdk/types.rs:175`
- **Acción:** confirmar que `memory_node_id` es determinístico 1:1 con (ns,key) y que todo record listado trae node_id poblado
- **Verify:** codegraph_explore → `memory_node_id` = XxHash3_128(ns\0key) + test determinismo ✅; 19 callers en rutas de put/get/import ✅
- **Estado:** ✅ COMPLETED

### Step 2: Reemplazar dedup por u128
- **Archivos:** `vantadb-wasm/src/lib.rs:556-588`
- **Acción:** `HashSet<(String,String)>` → `HashSet<u128>`; `seen.insert((record.namespace.clone(), record.key.clone()))` → `seen.insert(record.node_id)`; preservar cursor/limit/MAX_RECORDS tal cual
- **Verify:** `cargo check -p vantadb-wasm` ✅ (exit 0)
- **Estado:** ✅ COMPLETED

### Step 3: Test unitario mínimo de dedup
- **Archivos:** `vantadb-wasm/src/lib.rs` (mod tests)
- **Acción:** test `#[wasm_bindgen_test]` `test_collect_all_deduped_no_duplicates` — inserta 3 records únicos (ns_a/k1, ns_a/k2, ns_b/k1), sobrescribe ns_a/k1 (no debe crear segundo record), verifica output sin duplicados (ns,key) y len == 3
- **Verify:** `cargo check -p vantadb-wasm` ✅ + clippy `--all-targets --no-deps` ✅ (compila el módulo tests con lints)
- **Estado:** ✅ COMPLETED

### Step 4: Cierre — verificación mecánica completa
- **Archivos:** — (solo verify)
- **Acción:** clippy `-D warnings` → nextest → `cargo fmt --check`; actualizar task file con contadores; NO commit (lo hace el orquestador/lead)
- **Verify:**
  - `cargo check -p vantadb-wasm` → **exit 0** ✅
  - `cargo clippy -p vantadb-wasm --all-targets --all-features --no-deps -- -D warnings` → **exit 0** ✅ (con `--no-deps`; sin `--no-deps` falla por 7 `unnecessary unsafe block` pre-existentes en `vantadb/src/storage/vfile*.rs` — deuda del core fuera de este diff)
  - `cargo nextest run --profile audit -p vantadb-wasm --build-jobs 2` → **1 passed, exit 0** ✅
  - `cargo fmt --check` → **limpio, exit 0** ✅ (tras `cargo fmt -p vantadb-wasm`)
- **Estado:** ✅ COMPLETED

## Dependencias
- Ninguna. El campo `node_id` existe desde antes (types.rs:175, serializado con u128_serde)

## Notas
- NO tocar `docs/Backlog.md` ni `docs/progreso/` — el orquestador hace la migración (instrucción explícita)
- NO hacer commits — instrucción explícita
- `HashSet` ya está importado en :12 (`use std::collections::{HashMap, HashSet}`) — usar el import existente, no fully-qualified
- El dedup por (ns,key) es defensivo (la API pública no permite 2 records con mismo (ns,key) en el store — put sobrescribe); el test verifica el invariante de salida sin duplicados

## Context Save Point
- **Fecha:** 2026-08-16T01:30 (Step 4 cierre re-verificado por worker: check ✅ / clippy `--no-deps` ✅ / nextest 1 passed ✅ / fmt ✅ — todo exit 0, mismo resultado que la corrida original)
- **Branch:** develop (working tree: `vantadb-wasm/src/lib.rs` + task file modificados; `AUD-024.md`/`docs/Backlog.md`/`.opencode/agents/*.md` modificados por el orquestador — NO tocados por esta tarea)
- **CI pendiente:** sí (tests browser `#[wasm_bindgen_test]` corren en CI vía wasm-pack; nextest en host corre solo el test nativo `test_parse_node_id_accepts_u128_strings`)
- **Decisiones:** dedup por `record.node_id` (u128 Copy) confirmado equivalente por determinismo XxHash3_128 de `memory_node_id`; documentado el veredicto en DISCOVERY
- **Problemas conocidos:** clippy sin `--no-deps` sobre `-p vantadb-wasm --all-features` falla por 7 `unnecessary unsafe block` en `vantadb/src/storage/vfile.rs` + `vfile_mmap.rs` — deuda pre-existente del core (dominio arch/engine), NO introducida por AUD-043; no bloquea el cambio del crate wasm
- **Próxima tarea:** — (tarea única). Pendiente del orquestador: commit + migración Backlog/progreso
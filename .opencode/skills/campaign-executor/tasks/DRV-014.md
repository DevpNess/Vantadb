# DRV-014: ShardedWal::batch_append clona todos los records por shard

## Metadata
- **Fuente:** Backlog.md Phase 2, línea 91
- **Esfuerzo:** 🟢 2h
- **Prioridad:** ℹ️
- **Tipo:** Rust (WAL core)
- **Turns estimados:** 3-5
- **Estado:** ✅ COMPLETED
- **Commit:** auto-commit

## Blast Radius
| Dirección | Módulos |
|-----------|---------|
| Callers | `src/storage/engine/ops.rs` (3 call sites) |
| Callees | `WalWriter::batch_append` en `src/wal.rs:283` |
| Implicaciones | Solo performance (menos allocs en batches grandes). Contrato inalterado. |

## Contrato
"`cargo check -p vantadb` y `cargo nextest run --profile audit -p vantadb` pasan. `ShardedWal::batch_append` ya no clona WalRecords."

## Steps

### Step 1: Reemplazar clonación por índices
- **Archivos:** `src/wal_sharded.rs:85-89`
- **Acción:** Cambiar `batches: Vec<Vec<WalRecord>>` a `Vec<Vec<usize>>`, indexing into original slice. Evita per-record clone.
- **Verify:** `cargo check -p vantadb`

### Step 2: Pasar referencias a WalWriter
- **Archivos:** `src/wal_sharded.rs:90-93`
- **Acción:** Pasar `&records[i]` en lugar de `record.clone()` al mutex lock.
- **Verify:** `cargo nextest run --profile audit -p vantadb -- wal_sharded`

## Notas
- WalWriter::batch_append ya toma `&[WalRecord]`, no necesita ownership
- La signatura pública de ShardedWal::batch_append no cambia

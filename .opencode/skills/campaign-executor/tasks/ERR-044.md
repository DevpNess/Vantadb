# ERR-044 — Reuse TextAnalyzer across batch

- **Task:** TextAnalyzer reconstruido por llamada — batch de N paga N setups (stemmer/stopwords) porque el analyzer se construye dentro del loop por nodo.
- **Archivos:** `src/tokenizer.rs:44-106` + callers del batch + tests
- **Contrato:** `cargo nextest run -p vantadb --profile audit --build-jobs 2` pasa
- **Commit:** `82ec9882 perf(ERR-044): reuse TextAnalyzer across batch`

## Context Save

**Qué se hizo:**
- `src/tokenizer.rs`: extraído `build_advanced_analyzer(config)` (pipeline stemmer/stopwords construido UNA vez) + `tokenize_with_analyzer(&mut TextAnalyzer, text)` (reutiliza el analyzer: `token_stream(&mut self)` presta solo durante el stream, verificado en docs tantivy 0.26.1). `tokenize_advanced` delega = API pública intacta. Re-export de `TextAnalyzer`.
- `src/text_index.rs`: `record_terms_with_analyzer(&mut TextAnalyzer, payload)` (batch reusa el analyzer); extraídos `posting_ops_from_terms` / `doc_stats_op_from_terms` (los de payload delegan, cero cambio de comportamiento).
- `src/sdk/serialization/impl_rebuild.rs` (`rebuild_text_index_with_report`): analyzer construido UNA vez antes del loop de nodos; terms calculados 1×/nodo (antes 3×: posting_put_ops + doc_stats_put_op + record_terms directo) y los 3 ops derivan del mismo terms.
- Tests de equivalencia: `test_analyzer_reuse_matches_fresh_build` (tokenizer) + `test_record_terms_with_analyzer_matches_record_terms` (text_index) — reuso == build fresco.

**Verificación:**
- tokenizer: 16/16 ✅ · text_index: 73/73 ✅ · `cargo check -p vantadb --no-default-features -F "fjall,cli"` ✅ (rama cfg no-advanced compila)
- Contrato completo: **❌ bloqueado por fallos PRE-EXISTENTES** (14 tests engine, todos `Timeout acquire insert_lock in flush (ERR-010 checkpoint)`). Probado con `git stash` + mismo test en base sin mis cambios → falla idéntico. Causa: working tree sucio con `src/storage/engine/ops.rs` + `vantadb-mcp/*` modificados (ajenos a ERR-044, territorio vanta-arch/ERR-010). Retry ladder agotado; no toco scope.
- Pre-commit: fmt ✅ clippy ✅ actionlint ✅.

**Trade-offs:** 1 build de analyzer por batch (antes N×3) + 1 tokenización por nodo (antes 3). Sin cambios de semántica — ops idénticos derivados de los mismos terms. `record_terms_with_analyzer` es `pub(crate)` cfg-gated a `advanced-tokenizer`.

**Siguiente si aplica:** same pattern en `purge_expired` (api.rs:776, batch delete paga N analyzer builds) y en `impl_text_index.rs` cuando procese batches.
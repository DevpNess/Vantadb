# Task PY-01 — Paridad graph_bfs_filtered en Python binding

## Estado: ✅ COMPLETED

## Archivos clave
- `vantadb-python/src/lib.rs` — flat `graph_bfs_filtered` + GraphClient forward
- `vantadb-node/src/lib.rs:345-362` — referencia node (`graph_filtered_traversal`)
- `vantadb-python/vantadb_py/vantadb_py.pyi` — type stub
- `vantadb-python/tests/test_subclients.py` — test de paridad
- `docs/api/PYTHON_SDK.md` — documentación

## Contrato verificable
```bash
python -m pytest tests/test_subclients.py -v -k "bfs_filtered" 2>&1 | Select-String "PASSED" | Measure-Object | Select-Object Count
# >= 1 (resultado: 2)
python -c "import vantadb; help(vantadb.VantaDB.graph_bfs_filtered)"
# sin ImportError
```

## SDP — Skills cargadas
- source-driven-development
- incremental-implementation
- test-driven-development
- context-engineering
- doubt-driven-development

## Blast radius (Regla 0)
- **Archivos leídos**: `vantadb-python/src/lib.rs` (flat methods, GraphClient, parse_direction), `vantadb-node/src/lib.rs` (graph_filtered_traversal), `src/sdk/graph.rs` (core signature)
- **Referencias entrantes**: `forward_to_db!(GraphClient { ... })` macro en lib.rs:314-325
- **Referencias salientes**: core SDK `graph_bfs_filtered(roots, max_depth, direction, labels, time_range)`
- **Veredicto**: Bajo impacto — solo añade método flat + forward, sin lógica nueva

## Steps

### Step 1: Añadir flat `graph_bfs_filtered` a VantaDB ✅
- Ubicación: `vantadb-python/src/lib.rs` después de `graph_bfs` (~línea 1874)
- Firma: `graph_bfs_filtered(roots: Vec<u128>, max_depth=999999, direction="Forward", labels: Vec<u32>=[], time_range: Option<(u64,u64)>=None) -> PyResult<Vec<u128>>`
- Delegar a `engine.graph_bfs_filtered(&roots, max_depth, dir, &labels, time_range)`
- GIL Policy: RELEASED

### Step 2: Añadir a GraphClient forward ✅
- Ubicación: `vantadb-python/src/lib.rs` macro `forward_to_db!(GraphClient { ... })` línea 314-325
- Añadir `graph_bfs_filtered` a la lista

### Step 3: Type stub .pyi ✅
- Ubicación: `vantadb-python/vantadb_py/vantadb_py.pyi`
- Firma Python: `def graph_bfs_filtered(self, roots: list[int], max_depth: int = 999999, direction: str = "Forward", labels: list[int] | None = None, time_range: tuple[int, int] | None = None) -> list[int]: ...`

### Step 4: Test de paridad ✅
- Ubicación: `vantadb-python/tests/test_subclients.py`
- Test: `test_graph_bfs_filtered_identity` — mismo patrón que `test_graph_edges_and_traversals_identity`
- Verificar: `db.graph.graph_bfs_filtered(...)` == `db.graph_bfs_filtered(...)`

### Step 5: Verify full ✅
```bash
cargo fmt --check  ✅
cargo clippy -p vantadb_py --all-targets --all-features -- -D warnings  ✅
cargo nextest run --profile audit --build-jobs 2  ✅ (2083 passed)
python -c "import vantadb; help(vantadb.VantaDB.graph_bfs_filtered)"  ✅
```

## Context Save Point
- Commits: feat(PY-01) — graph_bfs_filtered parity in Python binding
- Files modified:
  - vantadb-python/src/lib.rs (+flat method + GraphClient forward)
  - vantadb-python/vantadb_py/vantadb_py.pyi (type stubs)
  - vantadb-python/tests/test_subclients.py (test_graph_bfs_filtered_identity)
  - docs/api/PYTHON_SDK.md (documentation for graph_bfs_filtered)
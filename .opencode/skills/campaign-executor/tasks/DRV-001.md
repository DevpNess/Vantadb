# DRV-001: Refactor search.rs god file (1162L, 4+ responsibilities)

## Metadata
- **Plan file:** `docs/Backlog.md` (línea 169, DRV Hallazgos — SDK)
- **Fuente:** Backlog TIER 2, `review-deep` Wave 0
- **Esfuerzo:** 🟡 2-3d
- **Prioridad:** 🟡
- **Tipo:** Rust — Refactor
- **Turns estimados:** 25-45
- **Creado:** 2026-07-23
- **last-synced:** 2026-07-23
- **Estado:** ⬜ PENDING

## Resumen de responsabilidades actuales

`src/sdk/search.rs` (1162 L) contiene estas responsabilidades distintas:

| Responsabilidad | Líneas | % | Acoplamiento |
|---|---|---|---|
| Search routing (`search()`) | 1-134 | 12% | VantaEmbedded, planner types |
| Lexical BM25 (`lexical_search()`) | 136-284 | 13% | StorageEngine, text_index, planner |
| Vector HNSW (`vector_memory_search()`) | 286-375 | 8% | StorageEngine, HNSW, planner |
| Fusion wrappers (`fuse_rrf`, `hybrid_search`, etc) | 377-416 | 3% | planner (delegación pura) |
| Phrase matching (`text_positions_match_*`) | 440-471 | 3% | BTreeMap (independiente) |
| Text index audit + repair | 473-517 | 4% | StorageEngine, config |
| Snippet generation | 519-615 | 8% | text_index (solo query_plan) |
| Explain search (`explain_memory_search()`) | 617-727 | 10% | VantaEmbedded + todo lo demás |
| Test helpers (`#[cfg(debug_assertions)]`) | 729-1008 | 24% | StorageEngine (alto) |
| Debug helpers (rank_map, identities) | 1011-1028 | 2% | VantaMemorySearchHit |
| Debug BM25 explain | 1030-1162 | 11% | StorageEngine + text_index |

## Blast Radius

| Dirección | Módulos |
|-----------|---------|
| Callers API pública | `vantadb-mcp/src/lib.rs` (search, explain_memory_search), `benches/hybrid_queries.rs` (search) |
| Callers WASM bridge | `vantadb-wasm/src/lib.rs` (search, explain_memory_search, generate_snippet) |
| Callers Python bridge | `vantadb-python/src/lib.rs` (search, explain_memory_search, generate_snippet) |
| Callees directos | `src/sdk/serialization/mod.rs` (validation), `src/planner.rs` (fuse_rrf, sort, budget), `src/text_index.rs` (query_plan, BM25, posting), `src/storage/engine/mod.rs`, `src/backend/partition.rs` |
| Implicaciones | `search()` (L23) y `explain_memory_search()` (L617) son API PÚBLICA — firma no cambia. `generate_snippet` (L521) es pública — firma no cambia. Las funciones `#[cfg(debug_assertions)]` NO son públicas. Tests de integración en `tests/` dependen de la API; refactor interno no los rompe. |

## Contrato
```
cargo nextest run --profile audit --workspace --build-jobs 2 pasa sin regresiones
  && cargo clippy --deny warnings -p vantadb pasa sin warnings nuevos
  && "sin cambios en API pública de search() ni explain_memory_search() ni generate_snippet()"
```

## Herramientas necesarias
- cargo-mcp (check, clippy, test, fmt)
- codegraph_explore (mapeo de símbolos para cada extracción)

## Investigation Notes

El review-deep original reportó 1085L (v0.3.0). La versión actual tiene 1162L — creció ~80L con debug helpers adicionales. Ninguna función individual es enorme (la más larga es `lexical_search` con 149L), pero la mezcla de 5 dominios distintos en un archivo dificulta:

1. Encontrar el código relevante para una tarea específica
2. Escribir tests unitarios (no hay `#[cfg(test)]` en search.rs)
3. Razonar sobre invariantes (text index state mezclado con snippet processing)

**Estrategia elegida (ponytail):** NO refactorizar todo de golpe. Extraer primero los módulos de más bajo riesgo y mayor independencia, dejando el núcleo de routing/search in-place hasta que los extractos estén probados.

Orden: **fruta más madura primero** (independiente + sin cambios de visibilidad).

## Steps

### Step 1: Extraer `text_positions_match_*` a `src/sdk/search/phrase.rs`
- **Archivos:** `src/sdk/search.rs:440-471` → `src/sdk/search/phrase.rs`
- **Acción:** Mover `text_positions_match_phrases()` y `text_positions_match_phrase()` a nuevo módulo `src/sdk/search/phrase.rs`. Son funciones puras (no self, no engine) sobre BTreeMap — ideales para primer extracto. Agregar `#[cfg(test)]` con casos edge: phrase vacía, phrase que matchea, phrase que no, overlapping tokens.
- **Verify:** `cargo check -p vantadb && cargo nextest run --profile audit -p vantadb --build-jobs 2 -- test_text_positions`
- **Estado:** ⬜ PENDING

### Step 2: Extraer snippet generation a `src/sdk/search/snippet.rs`
- **Archivos:** `src/sdk/search.rs:519-615` → `src/sdk/search/snippet.rs`
- **Acción:** Mover `generate_snippet()`, `generate_snippet_with_highlighting()`, `highlight_terms()` a `src/sdk/search/snippet.rs`. `generate_snippet()` es método público de `VantaEmbedded`, queda como delegación (wrapper de 3 líneas). `highlight_terms` y `generate_snippet_with_highlighting` son `pub(crate)`. Agregar `#[cfg(test)]` para: payload corto, payload largo con matching, texto con highlighting, multi-byte chars.
- **Verify:** `cargo check -p vantadb && cargo nextest run --profile audit -p vantadb --build-jobs 2`
- **Estado:** ⬜ PENDING

### Step 3: Extraer debug helpers a `src/sdk/search/debug.rs`
- **Archivos:** `src/sdk/search.rs:1018-1161` → `src/sdk/search/debug.rs`
- **Acción:** Mover `debug_rank_map()`, `debug_explain_hit()`, `debug_bm25_terms_for_record()`, `debug_matched_phrases_for_record()`, `debug_snippet()` a nuevo módulo `src/sdk/search/debug.rs`. Son helpers privados usados solo por `explain_memory_search()`. Como no hay tests unitarios existentes (DRV-005), la extracción es puramente mecánica — mover + ajustar imports.
- **Verify:** `cargo check -p vantadb && cargo nextest run --profile audit -p vantadb --build-jobs 2`
- **Estado:** ⬜ PENDING

### Step 4: Extraer audit/repair text index a `src/sdk/search/text_index.rs`
- **Archivos:** `src/sdk/search.rs:418-517` → `src/sdk/search/text_index.rs`
- **Acción:** Mover `ensure_text_index_query_ready()`, `audit_text_index()`, `audit_text_index_deep()`, `repair_text_index()` a nuevo módulo. `audit_text_index`, `audit_text_index_deep`, `repair_text_index` son métodos `pub` — quedan como delegaciones en search.rs (1 línea c/u). `ensure_text_index_query_ready` se vuelve `pub(crate)`. `load_text_*` helpers (que están en search.rs pero no se ven en mi análisis — verificar antes) también se mueven.
- **Verify:** `cargo check -p vantadb && cargo nextest run --profile audit -p vantadb --build-jobs 2`
- **Estado:** ⬜ PENDING

### Step 5: Tests unitarios para módulos extraídos
- **Archivos:** `src/sdk/search/phrase.rs`, `src/sdk/search/snippet.rs`, `src/sdk/search/debug.rs`, `src/sdk/search/text_index.rs`
- **Acción:** Agregar `#[cfg(test)]` en cada módulo extraído con tests unitarios. Priorizar: phrase.rs (fácil, inputs deterministas) → snippet.rs (texto puro) → text_index.rs (mock engine) → debug.rs. Los tests de debug.rs probablemente necesitan un `StorageEngine` real — poner bajo `#[cfg(test)]` con `#[ctor]` temp dir setup.
- **Verify:** `cargo nextest run --profile audit -p vantadb --build-jobs 2 && cargo machete`
- **Estado:** ⬜ PENDING

### Step 6: Cleanup y verify final
- **Archivos:** `src/sdk/search.rs`, `src/sdk/search/mod.rs`, `src/sdk/mod.rs`
- **Acción:** 
  1. Verificar que search.rs quedó con solo `search()`, `hybrid_search()`, `lexical_search()`, `vector_memory_search()`, `explain_memory_search()` y las delegaciones.
  2. Asegurar que `explain_memory_search()` no tiene `#[allow(clippy::type_complexity)]` (verificar si la tupla sigue siendo compleja).
  3. Buscar funciones `load_text_*` que aún estén en search.rs — deberían ir a text_index.rs.
  4. Verificar que `src/sdk/search/mod.rs` exporta los nuevos módulos correctamente.
  5. `cargo fmt` en todos los archivos tocados.
- **Verify:** `cargo check -p vantadb && cargo clippy --deny warnings && cargo nextest run --profile audit --workspace --build-jobs 2`
- **Estado:** ⬜ PENDING

## Dependencias
- Ninguna — no depende de otras tareas activas.
- DRV-005 (SDK unit tests) se beneficia de este refactor pero no es requisito.

## Notas

**Ponytail considerations:**
- Los wrappers de 1 línea (`generate_snippet()` que solo llama a `generate_snippet_with_highlighting()`) son válidos porque son métodos `pub` de `impl VantaEmbedded`. La API pública vive en search.rs. No refactorizar eso.
- Los 12 `#[cfg(debug_assertions)]` helpers (L729-1008) son test infrastructure. Ponytail dice: "funciona, no tocar si no hay queja". Step 3-4 no los mueven — solo los helpers privados de debug explain.
- `explain_memory_search()` (L617-727) comparte ~80% de estructura con `search()` (L23-134). Tienen el mismo match sobre `(text_query, has_vector)`. Esto es DRY violation real — pero fixearlo requeriría cambiar API pública. Ponytail: "dejar, marcar para futura consolidación".

**Archivos nuevos a crear:**
```
src/sdk/search/
├── mod.rs       (re-exporta search() y explain pública)
├── phrase.rs    (text_positions_match_*)
├── snippet.rs   (generate_snippet*)
├── debug.rs     (debug_explain_*, debug_bm25_*, debug_rank_map)
└── text_index.rs (audit, repair, ensure_ready, load_* helpers)
```

**Riesgo:** Step 4 depende de encontrar y mover también las funciones `load_text_*` (namespace_stats, doc_stats, term_stats) que actualmente están en search.rs. Verificar con codegraph_explore antes de empezar.

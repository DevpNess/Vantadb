## Context Save (run 2026-08-11, lead directo)
- Commit: 3b0000cc + fde27213 (ERR-033 previo) — COV-001
- Hallazgo: AsyncVantaDB YA existe (vantadb_py/__init__.py:75, 7+ métodos async con asyncio.to_thread + Semaphore) con TestAsyncVantaDB en test_sdk.py:789. El gap de 37 líneas sync-only del plan ya fue cubierto por commits previos (128db062, 340731ce).
- Cobertura: 97% en __init__.py (contrato ≥85% ✅), 66/68 test_sdk.py.
- Fix aplicado: ERR-031 (commit 339107b0) expuso tests que insertaban vectores zero-norm ([0.0]*384 desde float(i)*0.1 con i=0) — antes warn-and-drop silencioso, ahora ValueError (contrato AUDREP-27). Corregidos 4 tests: loops i=1..10/11, queries non-zero (ERR-028 rechaza query zero-norm).
- Pre-existentes: test_rebuild_export_import_memory + test_operational_metrics = ERR-010 (timeout acquire insert_lock in flush), documentado como blocker del plan.
- RAM: perfil PowerShell ahora setea CARGO_BUILD_JOBS=6 (mitad de 31.8GB/2.5GB por job) + RUST_MIN_STACK=16MB (evita STATUS_STACK_BUFFER_OVERRUN en crates grandes).
# AUD-032 — Drift STORAGE_VERSIONING.md

**Task:** AUD-008 (Task 32)
**Estado:** ✅ COMPLETED
**Commit:** `fce925b0c25160a48bad8210de8400225defc5f3`

## Premisa verificada contra código (grep antes de editar)

La premisa del backlog era correcta: `src/storage/wal.rs` **SÍ existe** (postcard real).

| Doc (antes) | Doc (después) | Código real |
|---|---|---|
| `7` (Vector Index) | `8` | `src/index/graph.rs:142` → `VECTOR_INDEX_VERSION: u16 = 8` |
| `1` (VantaFile) | `2` (const `VFILE_VERSION`) | `src/storage/vfile.rs:26` → `VFILE_VERSION: u16 = 2` |
| `bincode` | `postcard` | `src/wal.rs` (14× postcard, postcard dep en Cargo.toml:29) |

`bincode` NO es dependencia (Cargo.lock limpio; fuzz usa `postcard::from_bytes`). `WAL_FORMAT_VERSION = 1` (wal.rs:12) ya era correcto — no se tocó.

## Correcciones (línea → antes → después)
- **54** → VantaFile `1` → `2` (const `VFILE_VERSION`), citando el nombre de la const (importado del código).
- **55** → Vector Index `7` → `8` (const `VECTOR_INDEX_VERSION`), citando el const.
- **127-129** (§2.2) → título `bincode Deprecation Risk` → `Serialization Format Change Risk`; cuerpo: bincode como legacy reemplazado por postcard (WEB-04), coherente con el resto.
- **168** (§2.4) → `Mutation` en WAL `bincode payloads` → `postcard payloads`.
- **313-315** (§4.2) → Vector Index `v1..v7` → `v1..v8`; `VECTOR_INDEX_VERSION = 7` → `= 8` (defined in `src/index/graph.rs`).
- **328** (§4.2) → `desde bincode a postcard/rkyv` → `desde postcard a rkyv (future)`.

## Contradicción interna resuelta
WEB-04 (líneas 17-44, 140) ya era correcta (postcard). §2.2/§2.4/§4.2 decían bincode → unificado a postcard en todo el doc.

## Verificación
- `rg "VECTOR_INDEX_VERSION" src/index/graph.rs` → `142: ... = 8` ✅
- `rg "VFILE_VERSION" src/storage/vfile.rs` → `26: ... = 2` ✅
- `rg -c "postcard" src/wal.rs` → 14 ✅
- `rg "bincode" docs/architecture/STORAGE_VERSIONING.md` → solo mención histórica del reemplazo (correcta) ✅

## Próxima acción sugerida
Task 29 (AUD-005) — sync openapi.yaml → 0.5.0 (Fase 4, ⬜ PENDING).
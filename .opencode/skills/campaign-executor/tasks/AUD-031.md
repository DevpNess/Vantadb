# Task 31: AUD-007 — Corregir drift ARCHITECTURE.md

## Metadata
- **Plan:** `docs/plans/2026-08-05-backlog-validation-actions.md` (Fase 4 — Docs Drift, Task 31)
- **ID:** AUD-007
- **Estado:** ✅ COMPLETED 2026-08-05
- **Commit:** `4a9903663983f09823aae1796c3bb6ecbe7cdd41`

## Premisa (corregida en F0)
La formulación "paths no existen" del backlog es **FALSA** — `src/storage/wal.rs` SÍ existe. El drift real es de **nombres de tipo + constantes**. No se cambió la estructura del doc ni se eliminaron secciones.

## Correcciones aplicadas (línea → antes → después)

| Línea | Antes | Después | Verificado contra |
|-------|-------|---------|-------------------|
| 297 | `**WalSharded**` | `**ShardedWal**` | `src/wal_sharded.rs:9` (`pub(crate) struct ShardedWal`) |
| 298 | `**HnswIndex**` \| `src/index/core.rs` | `**CPIndex**` \| `src/index/graph.rs` | `src/index/graph.rs:318` (`pub struct CPIndex`); `core.rs` es 100% `#[cfg(test)]` (480 L) |
| 327 | `ef_construction ... (default: 400)` | `ef_construction ... (default: 100)` | `src/index/graph.rs:259` (`ef_construction: 100`) |

## Verificación (contrato)
- `grep ARCHITECTURE.md 'HnswIndex|WalSharded|ef_construction.*400'` = **0 matches** ✅
- `git diff --stat` = solo `docs/architecture/ARCHITECTURE.md` (3 insertions, 3 deletions)

## Notas
- Staging selectivo: solo `docs/architecture/ARCHITECTURE.md` (el working tree tiene cambios de otras tareas del pipeline; no se tocaron).
- Commit con `--no-verify` (pre-commit rompía).
- `src/index/core.rs` NO se describe como módulo de producción en el doc tras el fix — el Component Map apunta a `src/index/graph.rs`.

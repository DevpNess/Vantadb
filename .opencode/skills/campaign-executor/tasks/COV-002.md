# COV-002 — TS coverage medible + npm test verde

**Estado:** ✅ COMPLETED
**Commit:** `c9188639` — fix: repair maintenance deadlock, zero-norm search, putBatch versioning
**Fecha:** 2026-08-11
**Fuente:** plan 2026-08-09-residual-hardening.md Task 17 (ruta vanta-worker)

## Contrato (del plan)
- `npm test` en `vantadb-ts/` pasa (0 fallos)
- Coverage medible con c8 o vitest (v8) — **ya funciona** (68.77% stmts / 74.57% branch)

## DISCOVERY (2026-08-11, lead)
- `npm test` → **220/223 pass**, 3 fallos reales (no config):
  1. `src/__tests__/dx04.test.ts:341` — "putBatch deduplication (same key twice)":
     tras putBatch [`dup`→"first", `dup`→"second"], `db.get` devuelve payload "second"
     (correcto) pero `version` = 1 → esperado 2. El WASM no incrementa versión en dedup.
  2. `src/__tests__/integration.test.ts:88` — `expect(node!.edges[0].target).toBe(2n)`
     recibió `"2"` (string). Edge target llega como string, no bigint.
  3. `src/__tests__/load.test.ts:37` — `load` 5000 vectores: `putBatch: IO error:
     operation not supported on this platform` — operación WASM no soportada en Node.
- Coverage ya medible con config actual `vitest.config.ts` (provider v8, include src/**/*.ts,
  reportOnFailure) → **no tocar config de coverage**. Solo arreglar los 3 tests.
- vite-plugin-wasm está intencionalmente ausente (vitest-dev/vitest#6723); wasm se carga
  nativo por Node ≥22. NO reintroducir el plugin.
- Node v24.16.0 local; vitest 4.1.10; @vitest/coverage-v8 4.1.10.

## Pasos atómicos
1. **Investigar** cada fallo en el binding: `src/vantadb.ts` (putBatch, get, graph edges) y
   cómo el WASM (vantadb-wasm/pkg) devuelve versiones / edges / errors.
2. **Fix 3**: versión en putBatch dedup; normalizar edge target a bigint; IO error en load
   (verificar backend WASM en Node — quizás usa OPFS/fs no disponible → path de persistencia).
3. **No cambiar** el contrato del test — los tests reflejan el comportamiento correcto.
4. Verificar: `npm test` → 0 fail; `npm run build` (tsc) OK.

## Verificación (contrato de aceptación)
- `npm test` en `vantadb-ts/` → 223 pass, 0 fail
- `npx vitest run --coverage` → reporte v8 sin error (cualquier % overall, solo medible)
- `npm run build` → tsc sin errores nuevos

## Notas
- Errors WASM se envuelven en `src/errors.ts:52` (`wrapWasmError`) — el IO error raw del
  WASM hay que leerlo en la fuente Rust (vantadb-wasm) para entender "operation
  not supported on this platform".
- Es posible que el fallo 3 sea una limitación de plataforma del wasm build actual
  (no persistencia en load tests) → si es así, evaluar si el test debe usar backend
  in-memory explícito o si el binding necesita feature flag. Documentar decisión.

## CIERRE (2026-08-11, verificado por lead)
- `npm test` → **223/223 pass** ✅ (verificado post-commit)
- `npm run build` (tsc) → exit 0 ✅
- Coverage v8 medible: 69.41% stmts / 75% branch ✅
- `cargo check -p vantadb --no-default-features -F "fjall,cli"` → OK ✅

### Fixes aplicados por vanta-worker (commit `c9188639`)
1. **putBatch versioning en-batch** (`src/sdk/api.rs`): dedup UPSERT ahora incrementa versión
   dentro del batch (`Number(version) === 2` pasa) + `InsertMode::Incremental` en backends
   in-memory (wasm32 no soporta fs).
2. **u128 graph ids string ↔ bigint** (`vantadb-ts/src/types.ts`, `vantadb.ts`): edge targets
   normalizados a bigint en la frontera WASM.
3. **IO error en load test**: fix en `src/storage/engine/maintenance.rs` — `flush()` movido
   ANTES de adquirir `insert_lock` (lock no-reentrante; flush bajo el guard deadlockeaba native
   con Timeout 5s y panickeaba wasm32). `rebuildIndex` devuelve error limpio en vez de
   `unreachable` que corrompía el OpGate. + zero-norm → Euclidean en `_buildSearchRequest`.
- Tests Rust asociados: `cargo test -p vantadb --lib storage::engine::tests::maintenance`
  → 77/77 pass.
- Gotcha: el primer run bajo coverage fallaba por artefacto de build WASM (debug_assertions ON
  → `Instant::now()` panickeaba en wasm32); rebuild release target caliente lo resolvió.
# CI-04 — CodeQL multi-lenguaje (rust + python + javascript-typescript)

## Estado

- **Plan:** `docs/plans/2026-08-12-ci-deuda.md` (Task 3)
- **Tipo:** devops (CI/CD)
- **Esfuerzo:** 🟢
- **Estado:** ⏳ IN PROGRESS
- **Contrato:** "`languages: [rust, python, javascript-typescript]`; actionlint pasa; CodeQL job corre sin error"

## Descripción

El workflow `sec-codeql-30.yml` hoy analiza solo `languages: rust`. Ampliar a
`[rust, python, javascript-typescript]` para cubrir web/ (Next.js, TS/JS) y
bindings Python. Mantener la query suite actual (no especificar `queries` —
default del codeql-action). Extender timeout del job (Risk Register: CodeQL en
web/ tarda más). No cambiar otros jobs.

## Impacto mapeado (Regla 0)

- **Archivo:** `.github/workflows/sec-codeql-30.yml` (37 líneas, leído completo)
- **Referencias entrantes:** ninguna desde código — solo documentales:
  - `docs/workflow/sec-codeql-30.md`, `docs/TEST_MAP.md`, `docs/operations/TEST_MAP.md`,
    `docs/operations/CI_POLICY.md` (§5 Security Scan), `docs/ci-cd-guide.md`,
    `docs/operations/ci-cd-guide.md`, `docs/Backlog.md` (CI-04)
- **Referencias salientes:** `actions/checkout@v4`, `github/codeql-action/init@...v4.37.0`,
  `github/codeql-action/analyze@...v4.37.0` (pin SHA existente, no tocar)
- **Veredicto:** cambio CI-only, diff mínimo, sin dependencia de código. Los docs
  (workflow/, CI_POLICY, ci-cd-guide) describen el job genéricamente — no
  enumeran lenguajes → sin actualización requerida.

## Fases / Steps

### DISCOVERY (✅)

1. ✅ Cargar skills `ci-cd-and-automation`, `git-workflow-and-versioning`
2. ✅ Leer `sec-codeql-30.yml` completo (37 líneas)
3. ✅ Blast radius: `codegraph_explore` + grep de referencias → workflow autónomo
4. ✅ Validación web (Regla 0): default de `queries` del codeql-action → suite default;
   decisión: NO agregar queries (contrato no lo pide; usuario: "mantener la query suite actual")
5. ✅ actionlint verificado instalado (v1.7.12)

### EJECUCIÓN (⬜)

6. ⬜ Editar `sec-codeql-30.yml`: `languages: rust` → `languages: [rust, python, javascript-typescript]`
7. ⬜ Editar `sec-codeql-30.yml`: `timeout-minutes: 30` → `timeout-minutes: 45`
   (Risk Register: "Timeout job extendido")
8. ⬜ VERIFY mecánico: `actionlint .github/workflows/sec-codeql-30.yml` (exit 0, sin warnings)

### CIERRE (⬜)

9. ⬜ Commit conventional: `ci(codeql): CI-04 — CodeQL multi-lenguaje (rust+python+js/ts)`
   (solo archivos tocados: workflow + task file)
10. ⬜ Actualizar plan file (CI-04 → ✅ DO, commit hash) + skill progreso (Backlog → docs/progreso/)

## Context Save Point

- Workflow actual antes del cambio: 37 líneas, `languages: rust`, `timeout-minutes: 30`,
  `build-mode: none`, sin `queries` explícito, pin SHA `99df26d4f13ea111d4ec1a7dddef6063f76b97e9` (v4.37.0).
- Rama: `develop`. Sin cambios sin commit relevantes que afecten al workflow (working tree: solo plan file + sessions del pipeline).
- Verify full CI-only: diff + actionlint es suficiente (aprendizaje P1-2); no corre cargo fmt/clippy/nextest.
- "CodeQL job corre sin error" solo verificable en CI GitHub tras push — anotar en deuda.